{
  "name": "kaderisasi-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev:web-fe": "npm run dev --workspace web-fe",
    "dev:web-be": "npm run dev --workspace web-be",
    "dev:admin-fe": "npm run dev --workspace admin-fe",
    "dev:admin-be": "npm run dev --workspace admin-be",
    "build:web-fe": "npm run build --workspace web-fe",
    "build:web-be": "npm run build --workspace web-be",
    "build:admin-fe": "npm run build --workspace admin-fe",
    "build:admin-be": "npm run build --workspace admin-be"
  },
  "workspaces": [
    "./packages/*",
    "./apps/*"
  ]
}
